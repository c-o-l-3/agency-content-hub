{
  "name": "agency-content-site",
  "version": "1.0.0",
  "description": "Static site generator for Agency Content Hub",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "dependencies": {
    "marked": "^11.0.0",
    "gray-matter": "^4.0.3"
  }
}
